<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Fidelity" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Couleurs principales -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Surface / Background -->
        <item name="colorSurface">@color/surface</item>
        <item name="colorSurfaceVariant">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorOnSurfaceVariant">@color/text_light</item>
        
        <item name="android:colorBackground">@color/background</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Coins arrondis pour les dialogs Material -->
        <item name="shapeAppearanceMediumComponent">@style/ShapeAppearance.Fidelity.MediumComponent</item>
    </style>

    <style name="ShapeAppearance.Fidelity.MediumComponent" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerSize">16dp</item>
    </style>

    <!-- Thème pour les dialogues Material -->
    <style name="ThemeOverlay.Fidelity.Dialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="colorPrimary">@color/primary</item>
    </style>

    <!-- Thème sombre (night) -->
    <!-- Créer res/values-night/themes.xml avec les mêmes noms mais couleurs sombres -->

</resources>
